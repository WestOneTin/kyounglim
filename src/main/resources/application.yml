spring:
  profiles:
    active: local


# local
---
spring:
  profiles: local
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  h2:
    console:
      enabled: true
      path: /h2-console
  devtools:
    restart:
      enabled: true
